# VitalCheck

Product documentation lives in `docs/README.md`. Start there for setup, deployment, and architecture details.

Quick start:

```bash
npm install
npm install --workspace frontend
npm install --workspace functions
npm run dev
```

Frontend runs at http://localhost:5173 with Firebase emulators managed via the root dev script.

